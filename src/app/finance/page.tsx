import type { Metadata } from 'next'
import { EditorialList, EditorialPage } from '@/components/public/EditorialPage'
import { supabase } from '@/lib/supabase'
export const dynamic = 'force-dynamic'
export const metadata: Metadata = { title: 'Finance & Strategy | Vishnuraj Vishwakarma', description: 'Developing expertise in corporate finance, analysis, valuation, market analysis, negotiation, and business strategy.' }
export default async function FinancePage() { const { data } = await supabase.from('finance_items').select('*').eq('visible', true).order('order'); return <EditorialPage eyebrow="Developing Expertise" title="Finance & Strategy" intro="Building expertise in finance and strategy through PGDM coursework, company analysis, research, financial modeling, and practical projects. This is a learning portfolio, not a claim of professional financial advice."><div className="mb-12 flex flex-wrap gap-2">{['Company Analysis','Annual Report Analysis','Financial Ratios','Valuation','DCF Models','Industry Analysis','Investment Theses','Strategy Cases'].map(x => <span key={x} className="rounded-full border border-white/10 px-4 py-2 text-sm text-slate-300">{x}</span>)}</div><EditorialList items={data || []} /></EditorialPage> }
